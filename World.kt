class World(maxConnections: Int){
    val MAX_CONNECTIONS = maxConnections
    var isRunning: Boolean
    //I need a ConnectionContainer, Room array, main loop, Puppet Array
    //ConnectionContainer needs a function to kill all connection and return a value
    //to make sure we wait for it
}
